#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../sqwish/bin/sqwish" "$@"
  ret=$?
else 
  node  "$basedir/../sqwish/bin/sqwish" "$@"
  ret=$?
fi
exit $ret
