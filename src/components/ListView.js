import React from 'react';

import { Container, Row, Col } from 'reactstrap';

const ListView = ({ cards, ...props }) => (
  <Container {...props}>
    <Row>
      <Col>
      </Col>
    </Row>
  </Container>
);

export default ListView;
