name: Build Bot Deckbuild Lambda

on:
  pull_request:
    paths:
      - 'packages/botDeckbuildLambda/**'
      - 'packages/utils/**'
  push:
    branches:
      - 'master'
    paths:
      - 'packages/botDeckbuildLambda/**'
      - 'packages/utils/**'

permissions:
  contents: read

jobs:
  build-bot-deckbuild-lambda:
    if: github.repository == 'dekkerglen/CubeCobra'
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: npm install

      - name: Build Bot Deckbuild Lambda
        run: npm run build --workspace=packages/botDeckbuildLambda
