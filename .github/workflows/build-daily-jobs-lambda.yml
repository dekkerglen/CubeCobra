name: Build Daily Jobs Lambda

on:
  pull_request:
    paths:
      - 'packages/dailyJobsLambda/**'
      - 'packages/utils/**'
  push:
    branches:
      - 'master'
    paths:
      - 'packages/dailyJobsLambda/**'
      - 'packages/utils/**'

permissions:
  contents: read

jobs:
  build-daily-jobs-lambda:
    if: github.repository == 'dekkerglen/CubeCobra'
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: npm install

      - name: Build Daily Jobs Lambda
        run: npm run build --workspace=packages/dailyJobsLambda
