name: Build Recommender Service

on:
  pull_request:
    paths:
      - 'packages/recommenderService/**'
      - 'packages/utils/**'
  push:
    branches:
      - 'master'
    paths:
      - 'packages/recommenderService/**'
      - 'packages/utils/**'

permissions:
  contents: read

jobs:
  build-recommender-service:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: npm install

      - name: Build recommender service
        run: npm run build --workspace=packages/recommenderService
