extends layout

block content
  include flash
  br

  #react-root
  input#blogData(type='hidden', value=posts)
  input#cubeData(type='hidden', value=cubes)
  input#userid(type='hidden', value=user._id)
		
  script(src='/js/csrf.js')
  script(src='/js/dashboard.bundle.js')