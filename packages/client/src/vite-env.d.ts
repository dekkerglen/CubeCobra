/// <reference types="vite/client" />

// Vite's client types declare ambient modules for side-effect asset imports (`*.css`, images,
// `*.svg?react`, `import.meta.env`, etc.) — e.g. the global stylesheet imports in RenderToRoot.
