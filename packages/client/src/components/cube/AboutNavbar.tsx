import React from 'react';

const AboutNavbar: React.FC = () => {
  // Currently no special navbar needed for the About section
  // This component exists for future extensibility (similar to PlaytestNavbar)
  return null;
};

export default AboutNavbar;
