/** Shared limits used across all draft-simulator routes. */
export const MAX_DRAFTS = 500;
export const MAX_SEATS = 16;
/** Max total seats in a single simulation (MAX_DRAFTS × MAX_SEATS). */
export const MAX_TOTAL_SEATS = MAX_DRAFTS * MAX_SEATS; // 8000
export const MAX_HISTORY = 5;
