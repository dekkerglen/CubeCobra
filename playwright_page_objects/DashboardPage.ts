export class DashboardPage {
  page: any;
  loginLink2: any;

  constructor(page) {
    this.page = page;

    // this.loginLink2 = page.getByText('Login').first()
  }
  createCube = async () => {};
}
